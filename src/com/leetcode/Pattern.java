package com.leetcode;

public class Pattern {

    // Counting number of interval start and end;
    int[] Counting_Interval = new int[]{
    };


    int[] Parenthesis = new int[]{
    };

    int[] Adjacent_Graph_Nodes = new int[]{
            913
    };

    // Liner scan, then binary search, then truncate or replace
    int[] Liner_Scan = new int[]{
            85,
            115
    };

    // Calculate sliding window
    int[] Sliding_Window = new int[]{
    };

    // Look into corner or scan by row/column
    int[] Handle_Rectangle = new int[]{
            391
    };

    // for a in (1 : n)
    //   for b in (a + 1 : n)
    // Avoid permutation.
    int[] Find_Combination = new int[]{
    };
}
